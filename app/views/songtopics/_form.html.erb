<%= form_for(@songtopic) do |f| %>
  <% if @songtopic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@songtopic.errors.count, "error") %> prohibited this songtopic from being saved:</h2>

      <ul>
      <% @songtopic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :album %><br>
    <%= f.text_field :album %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
